
const dataType = {
  currency: 'currency',
  integer: 'integer',
  decimal: 'decimal',
  percentage: 'percentage',
  date: 'date',
  datetime: 'datetime',
  text: 'text',
  string: 'text',
  boolean:'boolean'
};

const editorType = {
  select: 'select',
};
export { dataType, editorType };

